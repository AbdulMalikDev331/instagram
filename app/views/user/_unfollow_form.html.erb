<div class="follow_<%= user.id %>" >
  <%= link_to "Unfollow", request_path(request),
                        method: :delete,
                        remote: true,
                        class: "btn btn-primary " %>
</div>
