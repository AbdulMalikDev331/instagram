$("#<%= "hide-box-#{@comment.id}" %>").replaceWith('<div class="mb-4  row" id="comment-<%= @comment.id %>"><div class="col-7 lb"><strong class="mr-3 "> <%= @comment.user.username%> </strong><span id = "this_comment_<%= @comment.id %>" > <%= @comment.comment%></span></div><div class="col-5"><span>&nbsp&nbsp&nbsp&nbsp</span><% if current_user && current_user.id == @comment.user.id  %><%= link_to "Edit", edit_post_comment_path(@comment.post_id, @comment),remote: true, class: 'btn btn-warning px-3 mr-1'%><% end %><% if current_user && current_user.id == @comment.user.id %><%= link_to "Delete", post_comment_path(@comment.post_id, @comment),method: :delete, remote: true, class: 'btn btn-danger px-3' %><%end%></div></div>');
