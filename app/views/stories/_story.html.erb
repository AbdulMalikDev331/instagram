<div class="col-12 mb-4">
  <% if story.user.profile_pic.attached? %>
    <%= link_to image_tag(story.user.profile_pic , class: "storythumbnail mr-5 border-green") , story_path(story.user.id)%>
  <% end %>
</div>
